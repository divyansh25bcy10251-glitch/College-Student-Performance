# College-Student-Performance
Interactive Gradio web app for predicting student performance using multiple ML algorithms (Random Forest, Decision Tree, Naive Bayes, KNN). Users can manage or auto-generate datasets, train models, export CSV, and input features to get predicted grades and expected final scores.
